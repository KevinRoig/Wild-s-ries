{% extends 'base.html.twig' %}
{% block title %}Episode #{{ episode.number }}{% endblock %}

{% block body %}
  <div>
    <h1>{{ season.program.title }}</h1>
  </div>
  <div>
   Year : {{season.year}}
  </div>
  <div>
    Season number : {{seasonId}} <br><br>
   </div>
  <div>
    <div>Episode number : {{ episode.number }}</div>
    <div><b>Title : {{ episode.title }}</b></div>
    <div>Synopsis : {{ episode.synopsis}}</div>
    <br><br>
</div>
<div>
  <a href="{{ path("program_show", { id: programId }) }}">Retour à la série</a>
</div>
<div>
<a href="{{ path('program_index') }}">
    Retour à la liste des programmes
</a>
</div>

{% endblock %}